<form method="post" action="{{ path('app_kudos_delete', {'id': kudo.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ kudo.id) }}">
    <button class="btn">Delete</button>
</form>
